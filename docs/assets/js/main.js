"use strict";const keys=document.querySelectorAll(".key");function playWav(e){const t=document.querySelector(`audio[data-key="${e.keyCode}"]`),n=document.querySelector(`.key[data-key="${e.keyCode}"]`);t&&(t.currentTime=0,t.play(),n.classList.add("playing"))}function removeTransition(e){"transform"===e.propertyName&&this.classList.remove("playing")}keys.forEach(e=>e.addEventListener("transitionend",removeTransition)),window.addEventListener("keydown",playWav);